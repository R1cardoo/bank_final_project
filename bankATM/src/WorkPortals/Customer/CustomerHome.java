package WorkPortals.Customer;

public class CustomerHome {
    // 1. show account info         (frame)
    // 2. deposit, withdraw, loan   (window)
    // 3. stockMarket Info          (frame)
    // 4. buy stock and sell stock  (window)
}
