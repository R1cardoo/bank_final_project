package WorkPortals;

import javax.swing.*;
import java.awt.*;

public class myButton extends JButton{
    public myButton(String name) {
        super(name);
        super.setPreferredSize(new Dimension(200, 60));
    }
}
